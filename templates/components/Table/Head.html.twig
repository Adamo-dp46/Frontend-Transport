{# @block content The default block #}
<th
    class="{{ 'h-12 px-4 text-left align-middle font-b [&:has([role=checkbox])]:pr-0 ' ~ attributes.render('class')|tailwind_merge }}"
    {{ attributes }}
> {# text-muted-foreground  #}
    {%- block content %}{% endblock -%}
</th>
